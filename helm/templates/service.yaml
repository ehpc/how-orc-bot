apiVersion: v1
kind: Service
metadata:
  name: {{ include "how-orc-bot.fullname" . }}
  labels:
    {{- include "how-orc-bot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "how-orc-bot.selectorLabels" . | nindent 4 }}
