apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "how-orc-bot.fullname" . }}-test-connection"
  labels:
    {{- include "how-orc-bot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "how-orc-bot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
